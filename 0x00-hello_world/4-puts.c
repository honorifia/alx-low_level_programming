#include <stdio.h>

/**
 * main -entry input
 * enter string
 * print with puts
 * Return: Always 0 success
 */
int main(void)
{
	char str1[] = "\"Programming is like building a multilingual puzzle";
	
	puts(str1);
	return (0);
}
